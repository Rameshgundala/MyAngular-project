import { TfCustomValidatorDirective } from './tf-custom-validator.directive';

describe('TfCustomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TfCustomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
